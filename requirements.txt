# ── Core AI SDKs ──────────────────────────────────────────
anthropic>=0.40.0
openai>=1.50.0
httpx>=0.27.0

# ── API Server ────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.1
python-multipart>=0.0.9

# ── Data Validation & Settings ────────────────────────────
pydantic>=2.8.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# ── State & Storage ───────────────────────────────────────
aiosqlite>=0.20.0
SQLAlchemy[asyncio]>=2.0.36
alembic>=1.14.0
asyncpg>=0.30.0
cryptography>=44.0.0

# ── Web Push (fire-alert broadcast) ───────────────────────
pywebpush>=2.0.0

# ── video_observer (.frames pipeline) ─────────────────────
opencv-python-headless>=4.10.0
numpy>=1.26.0
zstandard>=0.23.0

# ── Upgrade 1: Search Intelligence Stack ──────────────────
aiohttp>=3.10.0
beautifulsoup4>=4.12.0
trafilatura>=1.12.0          # full article extraction

# ── Upgrade 3: Code-verified Reasoning ────────────────────
sympy>=1.13.0                # symbolic math
scipy>=1.14.0                # numerical computation
z3-solver>=4.13.0            # constraint satisfaction

# ── Upgrade 4: DuckDB Analysis Agent ──────────────────────
duckdb>=1.1.0                # in-process SQL engine
pandas>=2.2.0                # DataFrame support

# ── Upgrade 6: Vector Memory (RAG) ────────────────────────
chromadb>=0.5.0              # vector database (local, free)
sentence-transformers>=3.2.0 # all-MiniLM-L6-v2 embeddings

# ── Utilities ─────────────────────────────────────────────
loguru>=0.7.2
tenacity>=9.0.0
aiofiles>=24.1.0
rich>=13.9.0
typer>=0.13.0
pillow>=11.0.0
prompt_toolkit>=3.0.0        # terminal input with history + arrow keys

# ── Remote Terminal (SSH) ─────────────────────────────────
asyncssh>=2.14.0             # SSH + SFTP client (ssh_connect, ssh_exec, ssh_upload, ssh_download)

# ── Dev / Testing ─────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
